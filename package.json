{
  "name": "unilibretour",
  "version": "0.1.0",
  "private": true,
  "description": "Museo Digital Interactivo — proyecto de grado",
  "scripts": {
    "dev": "echo \"Pendiente: levantar servidor en modo desarrollo\"",
    "build": "echo \"Pendiente: compilar frontend y backend\"",
    "build:frontend": "echo \"Pendiente: compilar TypeScript y Tailwind del frontend\"",
    "build:backend": "echo \"Pendiente: compilar TypeScript del backend\"",
    "start": "echo \"Pendiente: ejecutar servidor en producción\"",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}

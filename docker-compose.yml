# Entorno local: Flask + MySQL
services:
  db:
    image: mysql:8.0
    container_name: unilibretour-db
    restart: unless-stopped
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: museo_root
      MYSQL_DATABASE: museo_db
      MYSQL_USER: museo
      MYSQL_PASSWORD: museo
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./database/init:/docker-entrypoint-initdb.d
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pmuseo_root"]
      interval: 5s
      timeout: 5s
      retries: 10

  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: unilibretour-server
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      PORT: 5000
      DATABASE _URL: mysql+pymysql://museo:museo@db:3306/museo_db
      SECRET_KEY: toursecretkey
      JWT_SECRET_KEY: toursecretkey
    depends_on:
      db:
        condition: service_healthy
      restart: unless-stopped
    volumes:
      - .:/app

volumes:
  mysql-data:
